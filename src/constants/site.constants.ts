import { NAME } from '../config/app.config';

const site = {
  title: NAME,
  author: {
    name: 'Muhammad Qasim',
    location: 'London',
  },
  show: {
    navbar: true,
    footer: true,
  },
};

export default site;
